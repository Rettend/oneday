export default function Rules() {
  return (
    <section class="flex flex-col gap-4">
      <h1 class="text-2xl tracking-tight font-semibold">Rules</h1>
      <div class="text-muted-foreground">Regex rules list and test console coming soon.</div>
    </section>
  )
}
